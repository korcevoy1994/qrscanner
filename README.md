# 🎫 QR Scanner для билетов - Профессиональное решение

Мощное приложение для сканирования и валидации билетов с QR кодами. Построено с использованием **Next.js 16**, **ZXing** и **Supabase** с документацией, полученной через **MCP Context 7**.

## ⚡ Ключевые возможности

### 3 Способа Сканирования
1. **📷 Камера** - Сканирование в реальном времени
   - Автоматический выбор лучшей камеры
   - Поддержка переключения между камерами
   - Визуальная рамка сканирования с анимацией
   - Детальные сообщения об ошибках

2. **📁 Загрузка файла** - Сканирование из изображения
   - Поддержка JPG, PNG, GIF, WEBP
   - Предпросмотр изображения
   - Мгновенное распознавание

3. **⌨️ Ручной ввод** - Резервный вариант
   - Ввод номера билета вручную
   - Валидация в реальном времени

### Дополнительно
- ✅ Автоматическая валидация билетов в Supabase
- ✅ Защита от повторного использования
- ✅ Красивый градиентный UI/UX
- ✅ Адаптивный дизайн для всех устройств
- ✅ Детальная информация о билете
- ✅ Анимации и переходы
- ✅ TypeScript для надежности

## 🚀 Технологии

| Технология | Описание | Версия |
|-----------|----------|---------|
| **Next.js** | React фреймворк с App Router | 16.0.3 |
| **React** | UI библиотека | 19.2.0 |
| **TypeScript** | Типизированный JavaScript | 5.9.3 |
| **ZXing** | Профессиональная библиотека QR сканирования | Latest |
| **Supabase** | Backend-as-a-Service | Latest |
| **Turbopack** | Быстрый сборщик | Built-in |

## 📦 Установка

### 1. Клонирование и установка зависимостей

```bash
# Установка пакетов
npm install

# Установлено:
# - Next.js 16 + React 19
# - @zxing/browser + @zxing/library
# - @supabase/supabase-js
# - TypeScript
```

### 2. Настройка Supabase

#### Создание таблицы

Выполните SQL из файла `supabase-schema.sql`:

```sql
CREATE TABLE IF NOT EXISTS tickets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  order_id UUID NOT NULL,
  seat_id UUID NOT NULL,
  ticket_number TEXT NOT NULL UNIQUE,
  qr_code JSONB NOT NULL,
  status TEXT NOT NULL DEFAULT 'valid',
  used_at TIMESTAMP WITH TIME ZONE,
  metadata JSONB NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  event_id UUID NOT NULL
);

CREATE INDEX idx_tickets_ticket_number ON tickets(ticket_number);
CREATE INDEX idx_tickets_status ON tickets(status);
```

### 3. Переменные окружения

Создайте `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
```

## 🎯 Использование

### Запуск приложения

```bash
# Режим разработки
npm run dev

# Сборка для production
npm run build

# Запуск production
npm start
```

Откройте **http://localhost:3000**

### Как работает

#### Метод 1: Камера 📷
1. Откройте вкладку "Камера"
2. Нажмите "Начать сканирование"
3. Разрешите доступ к камере
4. Наведите на QR код билета
5. Автоматическая проверка и результат!

#### Метод 2: Файл 📁
1. Откройте вкладку "Файл"
2. Нажмите "Загрузить QR код из файла"
3. Выберите изображение
4. Автоматическое распознавание!

#### Метод 3: Ручной ввод ⌨️
1. Откройте вкладку "Ввод"
2. Введите номер билета
3. Нажмите "Проверить билет"

## 🏗️ Архитектура проекта

```
qrscanner/
├── app/
│   ├── api/
│   │   └── validate-ticket/
│   │       └── route.ts           # API endpoint
│   ├── globals.css                # Глобальные стили + анимации
│   ├── layout.tsx                 # Root layout
│   └── page.tsx                   # Главная страница (вкладки)
├── components/
│   ├── QRScanner.tsx              # 📷 Сканер с камеры (ZXing)
│   ├── QRFileUpload.tsx           # 📁 Загрузка файла
│   └── ManualInput.tsx            # ⌨️ Ручной ввод
├── lib/
│   └── supabase.ts                # Supabase клиент
├── types/
│   └── ticket.ts                  # TypeScript типы
├── .env.local.example             # Пример env
├── supabase-schema.sql            # SQL схема
└── README.md                      # Документация
```

## 🔌 API Reference

### POST /api/validate-ticket

Проверяет и активирует билет.

**Request:**
```json
{
  "ticket_number": "VOEV-203-I-16-1760711329863-5"
}
```

**Response (Успех):**
```json
{
  "success": true,
  "message": "Билет успешно активирован",
  "ticket": {
    "id": "uuid",
    "ticket_number": "VOEV-203-I-16-1760711329863-5",
    "status": "used",
    "used_at": "2025-11-15T15:00:00Z",
    "metadata": {
      "holder_name": "Nicolae Bondari",
      "holder_email": "email@example.com",
      "seat_zone": "203",
      "seat_row": "I",
      "seat_number": "16",
      "price": 550
    }
  }
}
```

**Response (Ошибка):**
```json
{
  "success": false,
  "message": "Билет уже использован 15.11.2025, 15:00:00",
  "ticket": { /* данные билета */ }
}
```

## 📝 Формат QR кода

QR код может содержать:

**1. JSON формат:**
```json
{
  "ticket_number": "VOEV-203-I-16-1760711329863-5",
  "order_id": "uuid",
  "seat_zone": "203",
  "seat_row": "I",
  "seat_number": "16",
  "holder_name": "Nicolae Bondari",
  "event_id": "uuid",
  "timestamp": 1760711329863
}
```

**2. Текстовый формат:**
```
VOEV-203-I-16-1760711329863-5
```

Приложение автоматически определяет формат!

## 🎨 Features в деталях

### ZXing Integration
- Используется `BrowserQRCodeReader` из `@zxing/browser`
- Поддержка всех форматов QR кодов
- Высокая скорость и точность распознавания
- Работает в современных браузерах

### UI/UX Highlights
- 🎨 Красивый градиентный дизайн
- 🔄 Плавные анимации и переходы
- 📱 Адаптивная верстка
- ⚡ Быстрая обратная связь
- 🎯 Интуитивный интерфейс
- ✨ Анимация пульсации сканера

### Обработка ошибок
- NotAllowedError → "Доступ к камере запрещен"
- NotFoundError → "Камера не найдена"
- NotSupportedError → "Камера не поддерживается"
- Детальные сообщения для пользователя

## 🔒 Безопасность

- ✅ HTTPS рекомендуется для production
- ✅ Row Level Security (RLS) в Supabase
- ✅ Проверка статуса перед обновлением
- ✅ Временные метки для аудита
- ✅ Валидация на сервере

## 🚀 Развертывание

### Vercel (Рекомендуется)
```bash
# Установка Vercel CLI
npm i -g vercel

# Деплой
vercel

# Production deploy
vercel --prod
```

Не забудьте добавить переменные окружения в Vercel Dashboard!

## 📚 Документация

Вся документация получена через **MCP Context 7**:
- Next.js 16 App Router
- ZXing JS Browser
- Supabase
- React 19
- TypeScript

## 🤝 Вклад

Разработано с использованием лучших практик **10x инженеров**:
- ✅ Множественные методы работы для надежности
- ✅ Профессиональная обработка ошибок
- ✅ Чистая архитектура
- ✅ TypeScript для безопасности типов
- ✅ Современные React patterns
- ✅ Оптимизация производительности

## 📄 Лицензия

ISC

---

**Сделано с ❤️ используя Context7 для получения актуальной документации**
# qrscanner
